# 実装タスク一覧（Vue + Vuetify + FastAPI）

## 1. フロントエンド基盤（Vue + Vuetify）

- [ ] タスク: Vue + Vuetify プロジェクトを構築する
  - 完了条件: `frontend` 配下に Vuetify を含む起動可能な構成が作成されている
  - バリデーション: フロントコンテナ起動でトップ画面が表示される

- [ ] タスク: ルーター構成を作成する
  - 完了条件: ログイン、備品一覧、備品登録、備品編集、ユーザー一覧、ユーザー登録、ユーザー編集、パスワード変更の各ルートが存在する
  - バリデーション: 画面遷移テストで全ルート遷移が成功する

- [ ] タスク: 一覧画面共通ナビゲーションを実装する
  - 完了条件: 備品一覧画面の上部に `ユーザー一覧へ`（管理者のみ）と`パスワード変更へ`（全ユーザー）を表示し、ユーザー一覧画面の上部に `備品一覧へ` と `パスワード変更へ` を表示する
  - バリデーション: ロール別表示テストで表示条件と遷移先が仕様通りであることを確認する

## 2. バックエンド基盤（FastAPI）

- [ ] タスク: FastAPI プロジェクトを構築する
  - 完了条件: `backend` 配下に `/api/` ルートを持つ起動可能なAPIが作成されている
  - バリデーション: `GET /api/health` が成功応答を返す

- [ ] タスク: Nginx リバースプロキシ設定を実装する
  - 完了条件: 静的配信と `/api/` のFastAPI転送が設定されている
  - バリデーション: ブラウザ経由で画面表示とAPI呼び出しが両方成功する

## 3. DB・永続化

- [ ] タスク: SQLiteスキーマ（asset_master, user_master, error_log）を実装する
  - 完了条件: 制約（PK/FK/UNIQUE/CHECK）を含むテーブルが起動時作成される
  - バリデーション: 重複登録や不正値がDB制約で拒否される

- [ ] タスク: リポジトリ層を実装する
  - 完了条件: 備品・ユーザー・エラーログのCRUDをリポジトリ経由で実行できる
  - バリデーション: リポジトリ単体テストが通過する

## 4. 認証・認可

- [ ] タスク: 認証APIを実装する
  - 完了条件: 正しいID/PWでログイン成功、誤った情報で失敗する
  - バリデーション: 認証APIテストが通過する

- [ ] タスク: ロール認可ガードを実装する
  - 完了条件: 一般ユーザーの更新系API呼び出しが拒否される
  - バリデーション: 権限違反テストで403が返る

- [ ] タスク: パスワードハッシュ保存を実装する
  - 完了条件: user_master に平文パスワードが保存されない
  - バリデーション: DB検証テストでハッシュ保存を確認する

## 5. 備品画面・備品API

- [ ] タスク: 備品一覧/備品管理画面を実装する
  - 完了条件: 一覧の各行末に `[編集] [削除] [貸出登録] [返却登録]` ボタンが表示される
  - バリデーション: 画面テストで行末ボタン表示と押下イベントを確認する

- [ ] タスク: 備品登録画面を実装する
  - 完了条件: 資産管理番号・備品名の入力で登録できる
  - バリデーション: 登録後に一覧へ反映される

- [ ] タスク: 備品編集画面を実装する
  - 完了条件: 備品名を編集して更新できる（資産管理番号は編集不可）
  - バリデーション: 更新後の一覧表示が変更される

- [ ] タスク: 備品API（一覧/登録/更新/削除/貸出/返却）を実装する
  - 完了条件: 仕様の全エンドポイントが実装される
  - バリデーション: API結合テストが全件通過する

## 6. ユーザー画面・ユーザーAPI

- [ ] タスク: ユーザー一覧画面を実装する
  - 完了条件: 一覧の各行末に `[編集] [削除]` ボタンが表示される
  - バリデーション: 行末ボタン表示と押下イベントを確認する

- [ ] タスク: ユーザー登録画面を実装する
  - 完了条件: ログインID・表示名・権限・初期PWで登録できる
  - バリデーション: 登録後に一覧へ反映される

- [ ] タスク: ユーザー編集画面を実装する
  - 完了条件: 表示名・権限を更新できる（ログインIDは編集不可）
  - バリデーション: 更新後に一覧へ反映される

- [ ] タスク: パスワード変更画面を実装する
  - 完了条件: 現在PW、新PW、新PW確認を入力して自己PW変更を実行できる
  - バリデーション: 変更後PWで再ログインできる

- [ ] タスク: ユーザーAPI（登録/更新/削除/自己PW変更/初期PW再設定）を実装する
  - 完了条件: 仕様の全エンドポイントが実装される
  - バリデーション: API結合テストが全件通過する

## 7. 貸出返却・制約

- [ ] タスク: 貸出登録処理を実装する
  - 完了条件: 貸出時に借用者・貸出日設定、状態を`貸出中`へ更新する
  - バリデーション: 状態遷移テストで検証する

- [ ] タスク: 返却登録処理を実装する
  - 完了条件: 返却時に借用者・貸出日クリア、状態を`貸出可能`へ戻す
  - バリデーション: 状態遷移テストで検証する

- [ ] タスク: 削除制約を実装する
  - 完了条件: 貸出中備品削除禁止、貸出保有ユーザー削除禁止を実装する
  - バリデーション: 異常系テストで409を確認する

## 8. ログ・運用

- [ ] タスク: エラーログ記録を実装する
  - 完了条件: 例外時に時刻・画面名・エラー内容を保存する
  - バリデーション: 強制例外テストでerror_log記録を確認する

- [ ] タスク: 90日超エラーログ削除バッチを実装する
  - 完了条件: 日次実行で90日超ログが削除される
  - バリデーション: 疑似ログ投入テストで削除確認する

- [ ] タスク: README運用記載を作成する
  - 完了条件: 起動方法・画面操作・再入力復旧手順をREADMEに記載する
  - バリデーション: README手順で再現起動できる

## 9. テスト実装

- [ ] タスク: 単体テストを実装する
  - 完了条件: 全サービス・リポジトリに正常系/異常系テストがある
  - バリデーション: `pytest` が通過する

- [ ] タスク: 結合テストを実装する
  - 完了条件: 全機能要件に対応するAPI結合テストがある
  - バリデーション: 結合テストが全件通過する

- [ ] タスク: 総合テストを実装する
  - 完了条件: 応答2秒以内・同時10人・ロール制御を検証できる
  - バリデーション: 総合試験結果が要件値を満たす

- [ ] タスク: E2Eテストを実装する
  - 完了条件: 要件シナリオ5件を100%網羅したPlaywrightテストがある
  - バリデーション: `docker compose run --rm test_playwright sh -c "npm install && npx playwright test"` が全件成功する

## 10. 全体確認

- [ ] タスク: RQ-IDと実装の対応を最終確認する
  - 完了条件: `docs/requirements.md` の全RQ-*（RQ-BK/RQ-BZ除く）が実装・テストでカバーされる
  - バリデーション: 対応表レビューで欠落0件を確認する

- [ ] タスク: 全変更点の実装完了確認を行う
  - 完了条件: 本tasks.mdの全タスクが完了し、未実装が0件である
  - バリデーション: 実装一覧、テスト結果、README記載を突合し差分0件を確認する
