name: isdd-post-implementation-review-proposals
skill: isdd-post-implementation-review
version: "1.0"
description: |
  isdd-post-implementation-review 向けの評価（案2: proposals.md 削除確認ルート）。
  docs/proposals.md が存在する状態で整合レビューを実行し、
  提案処理と proposals.md の削除が行えるかを検証する。

config:
  executor: mock
  model: auto
  trials_per_task: 1
  timeout_seconds: 300
  parallel: false

tasks:
  - tasks/*.yaml
