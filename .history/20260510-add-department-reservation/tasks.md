# 実装タスク一覧（部署名外部DB取得・予約機能追加）

## 1. データベース変更（SQLite）

- [ ] タスク: `asset_master` に返却予定日カラムを追加する
  - 完了条件: 貸出中レコードで返却予定日を必須にできるスキーマへ更新されている
  - バリデーション: マイグレーション後、返却予定日なしの貸出更新が拒否されることを確認する

- [ ] タスク: `reservation` テーブルを追加する
  - 完了条件: 予約ID、備品、予約者、開始日、終了日、予約状態を保持できる
  - バリデーション: 予約レコードの登録・取得・削除がDBで成功することを確認する

- [ ] タスク: 予約重複判定制約を実装する
  - 完了条件: 同一備品で両端含む期間重複が登録不可になっている
  - バリデーション: 境界日が同日のケース（例: 5/10-5/12 と 5/12-5/14）が拒否されることを確認する

## 2. 外部部署DB連携実装

- [ ] タスク: `external/neon-postgres/src` に部署名取得ラッパーを実装する
  - 完了条件: `login_id=user_id` で部署名を取得する読み取り専用関数が実装されている
  - バリデーション: 既知ユーザーで部署名が返り、未知ユーザーで `None` が返ることを確認する

- [ ] タスク: 外部DBアクセスを読み取り専用に固定する
  - 完了条件: 更新系SQLを発行しない実装および接続設定になっている
  - バリデーション: コードレビューで更新系SQLが存在しないことを確認する

- [ ] タスク: psycopgモックをE2E/テストで利用可能にする
  - 完了条件: mockモードで外部DB未接続でも部署名表示テストを実行できる
  - バリデーション: mockモード実行で部署名表示シナリオが通過する

## 3. バックエンドAPI変更（FastAPI）

- [ ] タスク: `GET /api/assets` を部署名付き一覧返却へ変更する
  - 完了条件: 備品一覧レスポンスに部署名と表示状態が含まれている
  - バリデーション: APIレスポンスで `取得しています...` / `部署名` / `部署名不明` の状態遷移を確認する

- [ ] タスク: 予約カレンダー取得APIを追加する
  - 完了条件: `GET /api/assets/{asset_number}/reservations` が月次予約情報を返す
  - バリデーション: 指定備品の予約期間・予約者・部署名・状態が返ることを確認する

- [ ] タスク: 予約登録APIを追加する
  - 完了条件: `POST /api/assets/{asset_number}/reservations` で期間指定予約を登録できる
  - バリデーション: 正常登録、重複拒否、返却予定日未設定拒否を確認する

- [ ] タスク: 予約取消APIを追加する
  - 完了条件: `DELETE /api/reservations/{reservation_id}` で本人または管理者が取消できる
  - バリデーション: 本人・管理者成功、第三者拒否（403）を確認する

- [ ] タスク: 貸出登録APIを返却予定日必須仕様へ変更する
  - 完了条件: `POST /api/assets/{asset_number}/loan` が返却予定日入力を必須化している
  - バリデーション: 返却予定日欠落時に貸出拒否されることを確認する

- [ ] タスク: 予約者優先貸出制御を実装する
  - 完了条件: 重複予約がある場合、該当予約者のみ貸出可能になる
  - バリデーション: 予約者一致で成功、不一致で409拒否を確認する

- [ ] タスク: 貸出時の予約状態遷移を実装する
  - 完了条件: 貸出成功時に対象予約が `貸出済み` へ遷移する
  - バリデーション: 貸出実行後に予約状態が更新されていることを確認する

- [ ] タスク: 返却時の貸出済み予約削除を実装する
  - 完了条件: 返却成功時に同一備品の `貸出済み` 予約のみ削除される
  - バリデーション: 返却後に対象予約のみ削除され、他予約は残ることを確認する

## 4. フロントエンド変更（Vue/Vuetify）

- [ ] タスク: 備品一覧画面へ部署名列と予約ボタンを追加する
  - 完了条件: 一覧行ごとに部署名表示と予約ボタンが表示される
  - バリデーション: 一般ユーザー・管理者ともに表示を確認する

- [ ] タスク: 予約カレンダー画面を新規追加する
  - 完了条件: 行末予約ボタンから別画面へ遷移し、月次カレンダーが表示される
  - バリデーション: 遷移、表示、戻る導線が動作することを確認する

- [ ] タスク: 予約登録UIを実装する
  - 完了条件: 開始日/終了日入力で予約登録できる
  - バリデーション: 正常登録、重複拒否、入力不正拒否を確認する

- [ ] タスク: 予約取消UIを実装する
  - 完了条件: 本人または管理者が予約取消操作できる
  - バリデーション: 権限ごとの表示制御と取消結果を確認する

- [ ] タスク: 部署名非同期表示を実装する
  - 完了条件: `取得しています...` から `部署名` または `部署名不明` へ遷移表示される
  - バリデーション: 正常取得・取得失敗で表示分岐することを確認する

## 5. テスト実装

- [ ] タスク: 予約関連バックエンド単体テストを追加する
  - 完了条件: 予約登録、重複判定、取消、貸出済み遷移、返却時削除の単体テストがある
  - バリデーション: 単体テストが全件通過する

- [ ] タスク: 外部部署取得ラッパーの単体テストを追加する
  - 完了条件: 既知ユーザー、未知ユーザー、外部取得失敗時のテストがある
  - バリデーション: mock使用で全件通過する

- [ ] タスク: API結合テストを追加する
  - 完了条件: 新規/変更APIの正常系・異常系が結合テストで網羅される
  - バリデーション: 結合テストが全件通過する

- [ ] タスク: E2Eシナリオを変更要件分追加する
  - 完了条件: 以下の5シナリオを追加し、既存と合わせて通過する
    - 返却予定日付き貸出と予約貸出済み遷移
    - 部署名非同期表示
    - 期間重複予約拒否
    - 予約カレンダー部署表示
    - 返却予定日未設定時予約拒否
  - バリデーション: mockモードE2E全件通過、realモード手動実行で主要シナリオ成功

## 6. 運用・設定更新

- [ ] タスク: 環境変数定義を更新する
  - 完了条件: 外部DB接続に必要な環境変数を `.env.example` に追記している
  - バリデーション: 新規開発環境で接続設定不足が発生しないことを確認する

- [ ] タスク: README運用手順を更新する
  - 完了条件: 予約機能操作、外部DB設定、mock/real実行手順を追記している
  - バリデーション: README手順どおりに再現できることを確認する

## 7. 全体確認

- [ ] タスク: 変更要件と変更設計の実装反映を総点検する
  - 完了条件: `change_requirements.md` と `change_detail_design.md` の全 [追加]/[削除] 項目に対応する実装・テストが揃っている
  - バリデーション: RQ-DS対応差分の欠落が0件であることをレビューで確認する

- [ ] タスク: 変更要件定義書と変更設計書を既存ドキュメントへ反映する
  - 完了条件: `.history/20260510-add-department-reservation/change_requirements.md` と `.history/20260510-add-department-reservation/change_detail_design.md` の確定内容を `docs/requirements.md` と `docs/detail_design.md` へ反映し、**既存ファイルが上書きされる**ことを明示したうえで更新している
  - バリデーション: 上書き後の `docs/requirements.md` と `docs/detail_design.md` が変更要件・変更設計と矛盾なく一致し、追加要件欠落が0件であることを確認する
